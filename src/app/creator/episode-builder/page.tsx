import EpisodeBuilderApp from "@/components/creator/episode-builder/EpisodeBuilderApp";

export const metadata = {
  title: "Episode Builder — Toonlora Studio",
  description:
    "Turn a business story into a full cartoon episode with in-depth research, AI planning, and image generation.",
};

export default function EpisodeBuilderPage() {
  return <EpisodeBuilderApp />;
}
