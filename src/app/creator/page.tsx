import CreatorStudioApp from "@/components/creator/CreatorStudioApp";

export const metadata = {
  title: "Toonlora Studio — Create & Publish Comics",
  description:
    "Build stories, design reusable characters, edit panels, and publish comic episodes on Toonlora.",
};

export default function CreatorPage() {
  return <CreatorStudioApp />;
}
